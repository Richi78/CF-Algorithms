#include <bits/stdc++.h>

using namespace std;

#define debug1(x) cout << #x << " = " << x << "\n";
#define debug2(x,y) cout << #x << " = " << x << " " << #y << " = " << y << "\n";
#define vdebug(a) cout << #a << " = "; for(auto x: a) cout << x << " "; cout << "\n";
#define int long long
#define FIO ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
#define all(v) v.begin(),v.end()
// const int MOD=998244353;

template<typename T> bool uin(T &a, T b) {return a>b?(a=b,true):false;}
template<typename T> bool uax(T &a, T b) {return a<b?(a=b,true):false;}

// Given N vertical and horizontal segments, check YES/NO if any intersection

const int START=1 , VERT=2 , END=3;
struct Event{
    int x;
    int type; // 1--> start    2--> vertical    3--> end
    int y1 , y2; // y2 only needed for vertical segment
    bool operator < (const Event &b) const {
        return make_pair(x, type) < make_pair(b.x, type);
    }
}

void solve(){
     
}

signed main(){
    FIO;
    // freopen("censor.in", "r", stdin);
    // freopen("censor.out", "w", stdout);
    // int tc;cin>>tc;
    // while(tc--)solve();
    solve();
}